package com.example.demo.calculator;
//Defines the core functionality that all calculators must implement.
public interface Calculator {
 double compute(String expression);  // All calculators must implement this.
}